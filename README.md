# Norah-Alhadyani
A simple, responsive HTML CV hosted via GitHub Pages.

The production entry point is `index.html`, so the site loads correctly at the repository root.
`cv.html` is kept as a redirect for older links.
